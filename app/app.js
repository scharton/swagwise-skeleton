(function(angular) {
    "use strict"; // tells IDE that strict JS syntax should be enforced
    var app = angular.module('Swagwise', ['ui.router','ui.bootstrap','ngResource','ngCookies','ngMessages']);
})(window.angular);

// last arg registers angular so the IDE doesn't complain that it doesn't recognize angular object

